const db = require('./db');
const userRepository = require('./userRepository');
const trainRouteRepository = require('./trainRouteRepository');

module.exports = {
  db,
  userRepository,
  trainRouteRepository
};